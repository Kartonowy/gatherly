export enum DialogKind {
    SleeveEdit,
    SleeveAdd,
    Settings,
    Auth,
    Placeholder, //TODO: Replace
    None
}

export enum Themes {
    Default = 'Default',
    Midnight = 'Midnight',
    None = 'None'
}

export enum Languages {
    English,
    Polish
}
