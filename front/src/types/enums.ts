export enum DialogKind {
    Sleeve,
    Settings,
    Auth,
    Board,
    None
}

export enum Themes {
    Default = 'Default',
    Midnight = 'Midnight',
    None = 'None'
}

export enum Languages {
    English,
    Polish
}
